
  # PNG to SVG

  This is a code bundle for PNG to SVG. The original project is available at https://www.figma.com/design/g3J8a4EfuGFMGS9LRYDF5M/PNG-to-SVG.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  